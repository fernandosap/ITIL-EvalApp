# Secret patterns scanned by the pre-commit hook (one regex per line).
# This file is loaded by .githooks/pre-commit at hook time.
# Lines starting with '#' are comments and are ignored.
# These patterns are intentionally broad — false positives are preferred
# over leaked credentials. Add a pattern below only if you are sure it
# catches a class of secret that none of the others do.
#
# NOTE: this file is itself scanned for matches against these patterns.
# It is excluded from the scan via the SECRETSCAN_IGNORE_FILE list in
# the hook script. If you add a new pattern here, do NOT add it as a
# literal on a single line that would match itself.

# Known credentials in this repo's history (do not commit again).
WelcomeWelcome[0-9]+\.
SAPacademy_[A-Za-z0-9_]+_2026!

# Common API key prefixes.
AKIA[0-9A-Z]{16}
sk-[A-Za-z0-9]{20,}
ghp_[A-Za-z0-9]{20,}
xox[baprs]-[A-Za-z0-9-]{10,}
glpat-[A-Za-z0-9_-]{20,}
AIza[0-9A-Za-z_-]{35}

# Private keys (header lines).
-----BEGIN [A-Z ]*PRIVATE KEY-----
-----BEGIN RSA PRIVATE KEY-----
-----BEGIN OPENSSH PRIVATE KEY-----
-----BEGIN EC PRIVATE KEY-----

# Connection strings with embedded credentials.
hana://[A-Za-z0-9_]+:[^@\s]+@
jdbc:hana://[A-Za-z0-9_]+:[^@\s]+@
https?://[A-Za-z0-9_]+:[^@\s]+@[A-Za-z0-9.-]+
